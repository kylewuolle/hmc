---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: clusteripamclaims.k0rdent.mirantis.com
spec:
  group: k0rdent.mirantis.com
  names:
    kind: ClusterIPAMClaim
    listKind: ClusterIPAMClaimList
    plural: clusteripamclaims
    singular: clusteripamclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Bound
      jsonPath: .status.bound
      name: bound
      type: string
    - description: Time elapsed since object creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterIPAMClaim is the Schema for the clusteripamclaims API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterIPAMClaimSpec defines the desired state of ClusterIPAMClaim
            properties:
              clusterDeploymentRef:
                type: string
              clusterNetwork:
                description: The allocation for requisitioning ip addresses for use
                  by the k8s cluster itself
                properties:
                  cidr:
                    description: Cidr notation of the allocated address space
                    type: string
                  ipAddresses:
                    description: IPAddresses to be allocated
                    items:
                      type: string
                    type: array
                type: object
              externalNetwork:
                description: The allocation for requisitioning ip addresses for use
                  by services such as load balancers
                properties:
                  cidr:
                    description: Cidr notation of the allocated address space
                    type: string
                  ipAddresses:
                    description: IPAddresses to be allocated
                    items:
                      type: string
                    type: array
                type: object
              nodeNetwork:
                description: The allocation requisitioning ip addresses for cluster
                  nodes
                properties:
                  cidr:
                    description: Cidr notation of the allocated address space
                    type: string
                  ipAddresses:
                    description: IPAddresses to be allocated
                    items:
                      type: string
                    type: array
                type: object
              provider:
                description: The provider that this claim will be consumed by
                type: string
            required:
            - clusterDeploymentRef
            type: object
          status:
            description: ClusterIPAMClaimStatus defines the observed state of ClusterIPAMClaim
            properties:
              bound:
                default: false
                description: flag to indicate that the claim is bound because all
                  ip addresses are allocated
                type: boolean
              clusterIPAMRef:
                type: string
            required:
            - bound
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
