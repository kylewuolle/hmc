{{ range $path, $_ :=  .Files.Glob  "files/templates/*.yaml" }}
{{- $content :=  tpl ( $.Files.Get $path ) (dict "Values" $.Values "TemplateName" "") | fromYaml }}
{{- $apiVersion := $content.apiVersion }}
{{- $kind := $content.kind }}
{{- $name := $content.metadata.name }}
{{- if not (lookup $apiVersion $kind $.Release.Namespace $name) }}
{{ tpl ( $.Files.Get $path ) (dict "Values" $.Values "TemplateName" $name) }}

---
{{- end }}
{{- end }}
