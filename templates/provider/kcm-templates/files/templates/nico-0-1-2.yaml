apiVersion: k0rdent.mirantis.com/v1beta1
kind: ServiceTemplate
metadata:
  name: nico-0-1-2
  annotations:
    helm.sh/resource-policy: keep
spec:
  helm:
    chartSpec:
      chart: nico
      version: 0.1.2
      interval: 10m0s
      sourceRef:
        kind: HelmRepository
        name: kcm-templates
