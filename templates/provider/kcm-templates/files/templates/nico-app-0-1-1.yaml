apiVersion: k0rdent.mirantis.com/v1beta1
kind: ServiceTemplate
metadata:
  name: nico-app-0-1-1
  annotations:
    helm.sh/resource-policy: keep
spec:
  helm:
    chartSpec:
      chart: nico-app
      version: 0.1.1
      interval: 10m0s
      sourceRef:
        kind: HelmRepository
        name: kcm-templates
